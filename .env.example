# Copy to .env or export these variables in your shell before running the LLM demo.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.6

# Optional: make benchmark output reproducible across machines.
ASD_AGENT_SEED=20260710

